// UC-A14: query the audit log by actor, target type, and time range.
export default function AuditLogPage() {
  return (
    <section>
      <h1>Audit Log</h1>
      <p>TODO (UC-A14): filterable table over GET /api/admin/audit-log.</p>
    </section>
  );
}
