// UC-A02: real-time station/locker/device/booking/revenue/incident snapshot.
// UC-A15: operational report generation (revenue, occupancy, verification success rate).
export default function DashboardPage() {
  return (
    <section>
      <h1>Operations Dashboard</h1>
      <p>TODO (UC-A02): fetch GET /api/admin/dashboard and render live station/locker/incident status.</p>
      <p>TODO (UC-A15): add a report export panel calling GET /api/admin/reports.</p>
    </section>
  );
}
