import 'package:flutter/material.dart';

/// UC-C06: Find Nearby Stations · UC-C07: View Station Details
/// UC-C08: View Locker Availability
class StationsPage extends StatelessWidget {
  const StationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TODO (UC-C06): map/list of nearby stations from GET /api/stations/nearby'),
      ),
    );
  }
}
