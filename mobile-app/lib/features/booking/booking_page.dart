import 'package:flutter/material.dart';

/// UC-C09: Create Booking · UC-C13: View Booking Details · UC-C14: View Booking History
/// UC-C15: Extend Booking · UC-C16: Cancel Booking
class BookingPage extends StatelessWidget {
  const BookingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TODO (UC-C09): size + time-slot picker, confirm, POST /api/bookings'),
      ),
    );
  }
}
