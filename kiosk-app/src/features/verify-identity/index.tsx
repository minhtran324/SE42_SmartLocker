// UC-K02: Verify Identity by Face — online only, calls the Face Recognition Engine via backend.
// UC-K03: Verify Identity by QR Code — works online and offline (signed payload check).
// UC-K04: Verify Identity by OTP or PIN — works online and offline.
export default function VerifyIdentityPage() {
  return (
    <section>
      <h1>Verify Identity</h1>
      <p>TODO (UC-K02/K03/K04): tabbed face/QR/OTP-PIN verification flow.</p>
    </section>
  );
}
